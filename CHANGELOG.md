# تغییرات

همه‌ی تغییرات مهم این کتاب در این فایل ثبت می‌شود. قالب بر اساس [Keep a Changelog](https://keepachangelog.com/fa/) و نسخه‌گذاری معنایی است.

## [Unreleased]

### اپ خالص Next.js (حذف Python و HTML ایستا)
- نسخهٔ آنلاین کتاب دیگر یک `index.html` ایستا نیست؛ روت `/book` از App Router با همان UI (نوار بالا، انتخاب فصل، فهرست کشویی، پیشروی اسکرول) همان URL قبلی (`/book/` + لینک `#ch-NN`) را سرو می‌کند.
- قالب `social.html` بنر به کامپوننت `SocialCard` تبدیل شد و از روت `/social-card` (noindex) برای بازتولید تصویر OG قابل اسکرین‌شات است.
- کل زنجیرهٔ Python حذف شد (`src/tools/`، `src/banner/`، `src/edition/`، `src/chapters/`)؛ فایل‌های PDF/EPUB ساخته‌شده در `public/pdf/` و `docs/pdf/` دست‌نخورده‌اند و سورس‌ها در تاریخچهٔ git محفوظ‌اند.
- زبان مخزن عملاً فقط TypeScript و CSS است: کانفیگ‌ها به `next.config.ts` و `postcss.config.ts` تبدیل شدند و خروجی `docs/` با `.gitattributes` از آمار زبان کنار گذاشته شد.
- لینک‌های خراب پیش‌نمایش README (`./docs/assets/…`) به `./assets/readme/…` اصلاح شد.

### ساختار مخزن (حذف CI و فایل‌های بلااستفاده)
- **`.github/` از مخزن بیرون رفت**؛ workflow ساخت کتاب حذف شد تا هر سه نسخه فقط محلی ساخته شود (۴ فایل در ریشه + `src/` و `docs/`). نتیجهٔ جانبی: صفحه‌بندی کتاب دیگر هیچ‌وقت توسط build خودکار روی runner جابه‌جا نمی‌شود.
- `docs/assets/banner.jpg` حذف شد: نسخهٔ ۲۲ کیلوبایتی بنر که هیچ‌جا به آن ارجاع داده نمی‌شد (صفحهٔ Pages و README هر دو `src/banner/banner.png` را نشان می‌دهند).
- `src/banner/banner.html` دیگر در مخزن نیست؛ خودِ `make_banner.py` آن را از روی `banner.css` تولید می‌کند، پس یک خروجی بود نه سورس. به `.gitignore` اضافه شد.
- `src/README.md` حذف شد و محتوایش (دستورات build، `--html`، کلیدهای front-matter، نحوی بلوک‌ها و ترتیب ساخت EPUB بعد از PDF) به `CONTRIBUTING.md` منتقل شد؛ یک فایل، یک مرجع.
- `src/tools/` حذف شد (`make_cover_bg.py` و پلاستر ۱٫۷ مگابایتی `cover-plate.png`)؛ `src/cover-bg.jpg` که `style.css` از آن استفاده می‌کند به‌عنوان سورس جلد در مخزن می‌ماند.

### ساختار مخزن (پاکسازی)
- پوشهٔ تودرتوی `react-19-persian-guide/` حذف شد: هر پنج فایلش (سه نسخهٔ کتاب + بنر و کارت اجتماعی) بایت‌به‌بایت همان چیزی بود که جای دیگری در مخزن وجود داشت. حالا **هر فایل، یک خانه** دارد؛ فایل‌های قابل دانلود فقط در `docs/pdf/` و بنر فقط در `src/banner/`.
- `LAUNCH.md` (کیت انتشار شخصی) از مخزن بیرون رفت؛ بخش‌های ماندگار آن — مراحل انتشار، تنظیمات Pages/Actions/Social preview — به `CONTRIBUTING.md` منتقل شد.
- لینک‌های README به مسیرهای جدید وصل شد و بلوک «ساختار» با درخت واقعی مخزن هم‌راستا شد.
- محتوای کتاب بدون تغییر: همان ۲۳۹ صفحه و ۴۰ سرفصلِ `v1.0.3`؛ سه فایل موجود با همان ضمیمه‌های Release ۱٫۰٫۳ یک‌سان‌اند (SHA-256 برابر).

## [1.0.3] — ۲۰۲۶-۰۹-۰۶

### صفحه‌بندی و کد
- پاراگرافِ توضیحِ بعد از هر پنجرهٔ کد/جدول/مقایسه هم اکنون با آن روی یک صفحه می‌ماند (`.code + p { break-before: avoid }`)؛ پیش‌تر فقط پاراگرافِ قبل از کد قفل بود.
- هیچ خط کدی دیگر در وسط شکسته نمی‌شود: ۳۶ خطِ بلندتر از ۸۶ کاراکتر در ۱۷ فصل بازنویسی شدند (تفکیک `import type`، شکستن آرگومان‌ها، ثابت‌های نام‌دار) و ممیزی `wrap_audit` صفر خط پیچیده گزارش می‌دهد. نسخهٔ چاپی هم عرض داخلی یکسانی با نسخهٔ رنگی دارد.
- نسخهٔ کتاب (`ویرایش ۱.۰.۳`) روی پانویس جلد، صفحهٔ پایانی و فراداده PDF (Keywords) چاپ می‌شود تا نسخهٔ قدیمی و جدید در هر نمایشگری قابل تشخیص باشد.
- ۲ صفحهٔ نیمه‌خالی دیگر (`tsconfig` در فصل ۱۵ و Motion در فصل ۳۲) با بازچینی پر شد.

### فهرست مطالب (بازطراحی مینیمال)
- طراحی ساده و خلوت در ۲ صفحه: فقط شمارهٔ فصل، عنوان، نقطه‌چین و شمارهٔ صفحه — بدون زیرعنوان، کارت، آمار یا برچسب اضافه.
- هر سطر یک خطِ واحد است؛ شمارهٔ فصل (راست) و شمارهٔ صفحه (چپ) در ستون‌های ثابت هم‌عرض و **دقیقاً روی خط کرسی عنوان** قرار می‌گیرند (۳۷ سطر، اختلاف کرسی ۰٫۰ pt، شمارهٔ صفحه‌ها همگی از یک x شروع می‌شوند).
- شمارهٔ صفحه‌ها با `target-counter` از خودِ سند محاسبه می‌شوند و با نشان‌گذاری PDF یکی هستند؛ همهٔ سطرها لینک داخلی‌اند، در نسخهٔ چاپی بدون زیرخط.
- با کوتاه‌شدن فهرست، فصل‌ها از صفحهٔ ۴ شروع می‌شوند؛ تعداد صفحات: ۲۳۹ (نسخهٔ چاپی ۲۴۰).

### جلد
- قرص‌های (pill) فناوری حذف شدند؛ به‌جای آن‌ها لوگوی رسمی هر فناوری روی یک هالهٔ نرم رنگی با نام زیر آن: React (اتم رسمی)، TypeScript (لوگوی رسمی `ts-logo-128`)، Vite (لوگوی رسمی ۲۰۲۶ با صاعقهٔ گرادیانی) و React Compiler (اتم بنفش با جرقه؛ نشان رسمی مستقلی ندارد). SVGها برای موتور WeasyPrint ساده‌سازی شده‌اند (بدون mask/filter) و در نسخهٔ چاپی هم خوانا هستند.
- شمارهٔ ویرایش از پای جلد برداشته شد (فقط «نسخه ۲۰۲۶»)؛ شمارهٔ دقیق ویرایش در صفحهٔ پایانی و فراداده PDF می‌ماند. فاصلهٔ جعبهٔ آمار تا کارت نویسنده ۱۰ میلی‌متر شد (قبلاً به هم چسبیده بودند).
- **پس‌زمینهٔ جلد**: پس‌زمینهٔ تمام‌صفحهٔ اختصاصی (`src/cover-bg.jpg`): اتم React با هندسهٔ دقیق لوگوی رسمی (سه بیضی ۱۱×۴٫۲ در زوایای ۰/۶۰/۱۲۰ درجه) به‌صورت خطوط نورانی فیروزه‌ای، کاملاً داخل صفحه (۸٫۵ میلی‌متر از لبهٔ بالا)، روی سرمه‌ای عمیق با ذرات، بوکه و هالهٔ بنفش. پشت ناحیهٔ متن‌ها پردهٔ تیرهٔ ملایم و پشت کارت نویسنده پس‌زمینهٔ نیمه‌کدر گذاشته شده تا خوانایی متن هرگز تحت‌تأثیر تصویر نباشد. نشان کوچک «React 19» و کد محو پس‌زمینه حذف شدند چون خودِ اتم نقش نشان را دارد. نسخهٔ چاپی بدون تصویر می‌ماند (جوهر کمتر) و نشان «React 19» را نگه می‌دارد.

### شماره‌گذاری صفحات و پاورقی
- شمارهٔ صفحه‌ها در پایین صفحات و در فهرست مطالب با **ارقام فارسی** (`counter(page, persian)` / `target-counter(…, persian)`) و قلم Vazirmatn چاپ می‌شوند؛ نشان‌گذاری PDF و تراز ستون فهرست (۳۷/۳۷) دست‌نخورده است.
- **پاورقی جدید (running footer):** یک خط نازک در عرض متن با سه بخش — سمت راست نشان ◆ و برچسب فصل جاری («فصل ۴ · JSX و رندر عناصر»، از عنوان فصل با `string-set`؛ در فهرست «فهرست مطالب» و در صفحهٔ پایانی «پایان مرجع»)، وسط شمارهٔ صفحه در یک قرص کوچک فیروزه‌ای، سمت چپ نام کتاب. دو فصل با عنوان بلند (۲۰ و ۲۹) عنوان کوتاه `short:` برای پاورقی دارند تا برچسب هرگز دوخطی نشود. جلد بدون پاورقی می‌ماند؛ نسخهٔ چاپی همان ساختار را خاکستری/مشکی دارد.

### بنر مخزن
- بنر README با همان ترکیب‌بندی مخزن Next.js 16 بازسازی شد: لپ‌تاپ با پنجرهٔ کد زنده، قرص‌های فناوری با لوگوی رسمی (React، TypeScript، Vite، Tailwind، Compiler)، چیپ‌های موضوعی، چک‌لیست ۱۲ موردی و کارت نویسنده با عکس واقعی. پس‌زمینه (`src/banner/plate.jpg`) و اسکریپت تولید (`src/banner/make_banner.py`) در مخزن هستند.

### سرفصل‌های داخل فصل
- تیترهای `h2` به‌جای خط عمودی سادهٔ کنار متن، یک میلهٔ گرادیانی فیروزه‌ای و یک نوار رنگی بسیار ملایم دارند که به سمت چپ محو می‌شود؛ تیترهای `h3` یک لوزی فیروزه‌ای کوچک. تزئین روی یک پوشش داخلی (`.hx`) اعمال می‌شود و اندازهٔ جعبهٔ تیتر عوض نشده، بنابراین صفحه‌بندی هر ۲۳۹ صفحه دقیقاً مثل قبل است (هیچ سطری جابه‌جا نشده). همان طرح در EPUB هم اعمال شد.

## [1.0.2] — ۲۰۲۶-۰۹-۰۶

### صفحه‌بندی (بازخورد خوانندگان)
- پنجره‌های کد **هرگز** بین دو صفحه شکسته نمی‌شوند؛ ۸ لیستینگ بلند (۳۵ تا ۶۳ خط) به دو پنجرهٔ `(1/2)` / `(2/2)` با یک جملهٔ پل تقسیم شدند و ساخت کتاب برای کد بلندتر از ۴۰ خط با خطا متوقف می‌شود.
- قانون «توضیح همراه کد» به فهرست‌ها و کارت‌های نکته هم گسترش یافت (`keep_with_next` در `build.py`).
- کارت نویسنده و پانویس جلد با `left + width` صریح جای‌گذاری شدند تا در هیچ نمایشگر PDFی از کادر بیرون نزنند؛ عکس نویسنده ۷۲px.
- فضای خالی میان‌فصل‌ها کم شد: ۹ صفحهٔ نیمه‌خالی با بازچینی محتوا (جابه‌جایی کارت نکته/جدول کنار کدِ مرتبط، افزودن پاراگراف معرفی برای `useRef` و Wizard، تقسیم `nginx.conf`) پر شد؛ کارت‌های مصاحبه/پروژه/جمع‌بندی هم دیگر بین دو صفحه شکسته نمی‌شوند.
- تعداد صفحات به ۲۳۷ رسید.

## [1.0.1] — ۲۰۲۶-۰۹-۰۶

### صفحه‌بندی و صفحه‌آرایی (PDF)
- **جلد:** کارت نویسنده و نوار پایین از کادر صفحه بیرون می‌زدند (تصویر نویسنده بریده می‌شد). علت ریشه‌ای: باگ WeasyPrint در جای‌گذاری عناصر `position: absolute` با `display: flex` در سند RTL. کانتینرهای absolute اکنون block هستند و flex داخل آن‌ها قرار دارد.
- **جدول‌ها دیگر بین دو صفحه شکسته نمی‌شوند** (`break-inside: avoid`). جدول‌های بلندتر از یک صفحه (واژه‌نامه، ۲۰ اشتباه رایج) به جدول‌های موضوعی کوچک‌تر با تیترهای فرعی تقسیم شدند.
- **پاراگراف معرفی هرگز از کد/جدول/نمودار خود جدا نمی‌شود:** پس‌پردازش `keep_with_next` در build.py به پاراگراف قبل از هر پنجره کد، جدول، ستون مقایسه و نمودار کلاس `keep-next` (`break-after: avoid`) می‌دهد.
- پنجره‌های کد تا ۳۰ خط همیشه یک‌تکه‌اند؛ پنجره‌های بلندتر با حداقل ۸ خط در هر طرف شکسته می‌شوند و سربرگ پنجره همیشه با بدنه می‌ماند.
- لیگاچرهای JetBrains Mono در کد خاموش شد (`=>`, `!==`, `->` اکنون به‌صورت تحت‌اللفظی رندر و در جستجو/کپی درست استخراج می‌شوند).
- آیکن جعبه «نکته» از `ⓘ` (بدون گلیف در فونت‌ها؛ fallback به فونت CJK) به `ℹ` تغییر کرد.
- ~۹۰ خط کد بلند بازچینی شد تا در عرض ستون/صفحه شکسته نشوند.

### محتوا
- React Router: به‌روزرسانی به **v8** (import `RouterProvider` از `react-router/dom`، یادداشت نسخه ۸ در فصل ۱۶).
- Zod 4 در فصل ۲۳ (`z.email()`, `error` به‌جای `message`, `z.flattenError`, `z.ZodType`).
- فصل ۲: قالب رسمی Vite 9 با oxlint؛ ESLint به‌عنوان افزودنی با `configs.flat.recommended`؛ حذف `baseUrl` منسوخ (TypeScript 6).
- فصل ۱۵: `RefObject` به‌جای `MutableRefObject` منسوخ؛ یادداشت گزینه‌های منسوخ TypeScript 6.
- فصل ۳۲: هشدار canary روی import `ViewTransition`؛ فصل ۲۴: `logError` به‌جای سایه‌انداختن روی `reportError` سراسری؛ فصل ۳۴: `HistoryState` به‌جای `History`.
- واژه‌نامه: افزودن `<ViewTransition>`، Middleware (Router) و Codemod؛ تقسیم به زیرجدول‌های موضوعی.

### صفحه معرفی (docs)
- `<main>` و لینک «پرش به محتوا»، استایل `:focus-visible`، پشتیبانی `prefers-reduced-motion`، `tabindex` روی ناحیه کد قابل‌اسکرول.
- `width`/`height` روی همه تصاویر (بدون CLS)، canonical، متاهای Twitter، `theme-color`، JSON-LD نوع Book، `robots.txt` و `sitemap.xml`.
- برند در عرض ≤۴۰۰px فشرده می‌شود تا نوار بالایی سرریز افقی نداشته باشد؛ `Production-Level` دیگر بین دو خط نمی‌شکند.
- تعداد صفحات به ۲۳۴ به‌روزرسانی شد.

## [1.0.0] — ۲۰۲۶-۰۹-۰۵

### انتشار اولیه
- **۳۷ فصل در ۴ بخش** بر پایه React 19.2، React Compiler 1.0، Vite 8، TypeScript 5
  - بخش یکم (۱–۱۸): بنیادها تا پروژه کامل مدیریت وظایف
  - بخش دوم (۱۹–۳۲): کارایی، مدیریت state، معماری، الگوها، فرم پیشرفته، خطا، a11y/RTL، امنیت، تست، Server Components، فریم‌ورک‌ها، استقرار، مهاجرت، انیمیشن و View Transitions
  - بخش سوم (۳۳–۳۵): ۲۵ نکته طلایی، کارگاه ۸ مینی‌پروژه، ۲۰ اشتباه رایج + چک‌لیست Code Review
  - بخش چهارم (۳۶–۳۷): ۴۵ پرسش مصاحبه (Junior تا Senior)، واژه‌نامه ~۱۲۰ اصطلاح
- سه قالب خروجی: PDF رنگی (۲۲۰ صفحه)، PDF چاپ سیاه‌وسفید، EPUB 3 (epubcheck-clean)
- صفحه معرفی GitHub Pages، بنر، کارت اجتماعی
- GitHub Actions برای build خودکار هر سه نسخه از سورس Markdown

[1.0.3]: https://github.com/rezaian-dev/react-19-persian-guide/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/rezaian-dev/react-19-persian-guide/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/rezaian-dev/react-19-persian-guide/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/rezaian-dev/react-19-persian-guide/releases/tag/v1.0.0
